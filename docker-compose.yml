services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "8081:8080"
    environment:
      - GEMINI_API_KEY=AIzaSyBBcldNRkYY3iwrtfCJdRf_orRgmJ6E4HU
      - DATABASE_URL=postgresql://postgres.yeovwrgepgdtrjqpkljb:LogFlow2026@aws-1-ap-south-1.pooler.supabase.com:6543/postgres
      - PORT=8080
    restart: unless-stopped

  agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    environment:
      - SERVER_URL=http://server:8080
    volumes:
      - ./app.log:/app/app.log:ro
    depends_on:
      - server
    restart: unless-stopped
